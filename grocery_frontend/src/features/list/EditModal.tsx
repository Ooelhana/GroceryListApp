interface EditModalProps {
  isEditOpen: boolean;
  setIsEditOpen: (arg0: boolean) => void;
  selectedItem: number;
}

export default function EditModal({
  selectedItem,
  isEditOpen,
  setIsEditOpen,
}: EditModalProps) {
  return null;
}
